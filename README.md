# taxin's dotfiles
# Setup

## 1. create .gitconfig.local
```bash
touch .gitconfig.local
vi .gitconfig.local
```

```
[user]
	name = <git_username>
	email = <git_email_address>
```

## 2. make symbolic link
```
make link
```